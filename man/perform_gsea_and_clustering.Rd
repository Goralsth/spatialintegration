% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perform_gsea_and_clustering.R
\name{perform_gsea_and_clustering}
\alias{perform_gsea_and_clustering}
\title{Perform GSEA, Clustering, and Visualization with NMF on a Seurat Object}
\usage{
perform_gsea_and_clustering(
  seurat_object,
  gsea_category = "C7",
  umap_dims = 1:30,
  clustering_resolution = 0.5,
  max_terms_per_factor = 3
)
}
\arguments{
\item{seurat_object}{A Seurat object with NMF reduction calculated.}

\item{gsea_category}{The category for GSEA (e.g., "C7"). Default is "C7".}

\item{umap_dims}{The number of dimensions to use for UMAP. Default is 1:30.}

\item{clustering_resolution}{The resolution for clustering. Default is 0.5.}

\item{max_terms_per_factor}{The maximum number of terms to display per factor in GSEA heatmap. Default is 3.}
}
\value{
A list containing the updated Seurat object and a ggplot comparing NMF and PCA clusters.
}
\description{
This function runs GSEA using NMF factors, clusters the data, and generates visualizations
including heatmaps, UMAP, and feature plots. It also compares NMF clusters with PCA clusters.
}
\examples{
library(Seurat)
library(ggplot2)
seurat_object <- process_and_run_nmf(seurat_object, nmf_rank = 5)
results <- perform_gsea_and_clustering(seurat_object)
results$comparison_plot
}
